<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h4>OD Request Rejected</h4>
    <p>
      You have one OD request is Rejected From
      <% @employee = Employee.find(@od_request.first_reporter_id) %>

      Employee ID : <%= @manager.try(:manual_employee_code) %><br>
      Employee Name : <%= @manager.try(:first_name)+' '+@manager.try(:middle_name)+' '+@manager.try(:last_name) %><br>

      For Employee <br>
      Employee ID : <%= @od_request.employee.try(:manual_employee_code) %><br>
      Employee Name : <%= @od_request.employee.try(:first_name)+' '+@od_request.employee.try(:middle_name)+' '+@od_request.employee.try(:last_name) %><br>
      From <%= @od_request.start_date.to_date %> To <%= @od_request.end_date.to_date%><br>
       
  </body>
</html>
