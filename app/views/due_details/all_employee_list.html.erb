<div class="box">
  <div class="box-header">
    <h3 class="box-title">Employee List for Assigning No Due Template</h3>
    <div class="pull-right">
    </div>
  </div><!-- /.box-header -->
  <div class="box-body">
    <table id="example6" class="table table-bordered table-hover table-condensed">
    <thead>
      <tr>
      <th>ID</th>
      <th>Employee Name</th>
      <th>Status</th>
      <th>Action</th>
    </tr>
  </thead>

  <tbody>
    <% @employees.each do |employee| %>
      <tr>
        <td><%= employee.manual_employee_code %></td>
        <td><%= full_name(employee) %></td>
        <td><%= employee.try(:status) %></td>
        <td>
        <!-- < emp=Employee.ids %> -->
<!-- 	      < emp.each do |e| %> -->
	      <!-- < due_emp_details = DueEmployeeDetail.where(employee_id: emp).pluck(:employee_id).map {|i| i.to_i} %> -->
<!--           < due_emp_details.each do |ded| %>
 -->             
         <!--  <debugger%> -->
<!--             ['Cat', 'Dog', 'Bird'].include? 'Dog'
 -->         <!-- <  if emp.include? due_emp_details.uniq %> -->
<!--              < if emp.any? {|i| due_emp_details.uniq.include?(i) } %>
 -->
            
<!--             < else %>
 -->            
          <% if have_due_template(employee) %>
                <%= link_to '', all_due_template_due_details_path(emp_id: employee.id),class: 'btn btn-xs btn-info btn-xs fa fa-tasks' %>
              <% else %>
                <%= link_to ' View Template', due_employee_detail_list_due_details_path(emp_id: employee.id),class: 'btn btn-xs btn-primary' %>
           <% end %>
<!--          < end %>
 -->        </td>
        </tr>
      <% end %>
        </tbody>
  </table>
</div>
</div>

<script type="text/javascript">
$(document).ready(function() {
    $('#example6').DataTable( {
        "lengthMenu": [[10, 25, 50,100, -1], [10, 25, 50,100, "All"]],
        "scrollX": true
    } );
} );
</script>