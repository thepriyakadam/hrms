<%= form_for(@employee_designation) do |f| %>
  <% if @employee_designation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employee_designation.errors.count, "error") %> prohibited this employee_designation from being saved:</h2>

      <ul>
      <% @employee_designation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
