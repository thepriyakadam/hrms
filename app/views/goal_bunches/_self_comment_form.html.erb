<div class="modal-dialog">
  <div class="modal-content">
    <div class="modal-header">
      <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
      <h4 class="modal-title"><center> Self Evaluation </center></h4>
    </div>
    <div class="modal-body">

      <%= bootstrap_form_for :goal_bunches,url:{action:'self_comment',emp_id: @employee.id,goal_bunch_id: @goal_bunch_id.id} do |f| %> 
      <%= hidden_field_tag 'goal_rating_ids[]',"",value: @goal_rating.id %>
<!-- 
        <table class="table table-bordered table-hover table-condensed">
          <thead>
            <tr>
              <th colspan="3"><%#= @goal_bunch_id.try(:period).try(:name) %></th>
              <th colspan="3"><%#= @goal_bunch_id.employee.manual_employee_code %> : <%#= full_name(@goal_bunch_id.try(:employee))%>
              </th>
            </tr>
            <tr>
              <th>Goal Perspective</th>
              <th>Measure</th>
              <th>Activity</th>
              <th>Target/Unit</th>
              <th>Weightage</th>
              <th>Aligned</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td><%#= @goal_rating.goal_perspective.try(:name) %></td>
              <td><%= @goal_rating.goal_measure %></td>
              <td><%= @goal_rating.activity %></td>
              <td><%= @goal_rating.target %></td>
              <td><%= @goal_rating.goal_weightage %></td>
              <td><%= @goal_rating.aligned %></td>
            </tr>
          </tbody>
        </table> -->

        <div class="row">
          <div class="col-sm-9">
            <label>Comment</label>
            <%= text_area_tag 'appraisee_comment[]',"",class: "form-control",autofocus: true %>    
          </div>
          <div class="col-sm-3">
            <label>Rating</label>
            <%= select_tag 'appraisee_rating[]',options_for_select(all_rating_type),{class: "form-control", hide_label: true,include_blank: " Select Rating"} %>
          </div>
        </div>
        <div class="row">
          <div class="col-sm-12" style="padding-top:20px;">
            <center><%= f.submit "Create", class: 'btn btn-success btn-sm' %></center>
          </div>
        </div>
      <% end %>

  	</div>
    <div class="modal-footer"></div>
  </div><!-- /.modal-content -->
</div><!-- /.modal-dialog -->
	