<%= form_for(@interview_attribute) do |f| %>
  <% if @interview_attribute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@interview_attribute.errors.count, "error") %> prohibited this interview_attribute from being saved:</h2>

      <ul>
      <% @interview_attribute.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
