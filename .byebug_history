exit
@goal_bunches = GoalBunch.find(params[:id])
exit
goal_bunch_ids = params[:goal_bunch_ids]
@goal_bunches = GoalBunch.all
continue
@qualifications = Qualification.where(employee_id: @employee.id)
@employees = Employee.where(id: @employee.id).group(:id)
@goal_bunch = GoalBunch.find(params[:id])
@employee = Employee.find(params[:emp_id])
exit
@goal_bunch_ids = params[:goal_bunch_ids]
 params[:goal_bunch_ids]
exit
@goal_rating = GoalRating.find(appraiser_id: goal_rating.id)
exit
@goal_rating = GoalRating.find(id)
    mail(to: interview_schedule.email_id, subject: 'Interview Rechedule')
    @employee = Employee.find(@interview_schedule.employee_id)
    @interview_schedule = InterviewSchedule.find(interview_schedule.id)
    @interview_schedule = interview_schedule
 def sample_email_to_candidate(interview_schedule)
exit
@goal_rating = GoalRating.find(goal_rating.id)
exit
@employee = Employee.find(@goal_rating.appraisee_id)
@goal_rating = goal_rating
@employee = Employee.find(params[:emp_id])
exit
@employee = Employee.find(params[:emp_id])
