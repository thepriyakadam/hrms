<%= form_for(@bonus_employee) do |f| %>
  <% if @bonus_employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bonus_employee.errors.count, "error") %> prohibited this bonus_employee from being saved:</h2>

      <ul>
      <% @bonus_employee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :employee_id %><br>
    <%= f.text_field :employee_id %>
  </div>
  <div class="field">
    <%= f.label :bonus_date %><br>
    <%= f.date_select :bonus_date %>
  </div>
  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
