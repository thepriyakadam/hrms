json.extract! bonus_employer, :id, :base_component, :limit_amount, :percentage, :effective_from, :effective_to, :is_active, :is_confirm, :created_at, :updated_at
json.url bonus_employer_url(bonus_employer, format: :json)