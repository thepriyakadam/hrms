<div class="box box-default">
  <div class="box-header with-border">
    <h3 class="box-title">Feedback :</h3>
    <div class="pull-right"></div>
    <div class="box-body">
      <%= bootstrap_form_for :employee_plan, url:{ action: "feedback", employee_plan_id: @employee_plan.id, id: 'employee_plan' }  do |f| %>
        <div class="row">
          <div class="col-sm-5">
            <div class="form-group required">
              <%= f.text_field :feedback, label:'Feedback' %>
            </div>
          </div>
          <div class="col-sm-3">
            <div class="actions" style="padding-top: 20px;">
              <%= f.submit 'Submit', class: "btn btn-primary btn-sm"%>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
<script>
  $("#employee_plan").validate({
    rules: {
      "employee_plan_id[feedback]":{
        required: true,
      },
    },
    messages: {
      "employee_plan_id[feedback]":{
        required: "Please Specify The Feedback",
      },
    },
    errorPlacement: function(error, element) {
     error.css({"color": "red", "font-size": "12px","font-weight" : "normal"})
     error.insertAfter(element.parent(element));
    }
  });
</script>
