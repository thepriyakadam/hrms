<label class="control-label required" for="company_district" aria-required="true">Member</label>
	<%= select :issue_requests,:issue_tracker_member_id,(@issue_tracker_members.present? ? (@issue_tracker_members.collect {|b| [b.employee.try(:first_name) +' '+b.employee.try(:middle_name)+' '+b.employee.try(:last_name),b.id]}) : {}),{},{onchange:"var a={group_id:$(this).val(), datea: #{@datea}, issue_member_id: #{@group_id}}; $.get('/issue_requests/memberwise_report_list',a,function(response){});",class: 'form-control'} %>
