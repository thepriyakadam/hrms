<table id="example2" class="table table-bordered table-hover table-condensed">
  <thead>
    <tr>  
      <th>Name</th>
      <th>Action</th>
    </tr>
  </thead>
  <tbody>
  <% @goal_perspectives.each do |goal_perspective| %>
      <tr>
        <td><%= goal_perspective.name %></td>
        <td>
          <%= link_to '', edit_goal_perspective_path(goal_perspective),class: 'btn btn-xs btn-success fa fa-edit',remote:true %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>