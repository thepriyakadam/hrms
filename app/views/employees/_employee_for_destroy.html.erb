
  <table id="example1" class="table table-bordered table-hover table-condensed">
    <thead>
      <tr>
        <th>Employee ID</th>
        <th>Employee Name</th>
        <th>Designation</th>
        <th>Employement</th>
        <th>Action</th>
      </tr>
    </thead>
    <tbody>
        <tr>
          <td><%= @employee.manual_employee_code %></td>
          <td><%= @employee.first_name %><%= @employee.middle_name %><%= @employee.last_name %></td>
          <td><%= @employee.joining_detail.try(:employee_designation).try(:name) %></td>
          <td><%= @employee.employee_type.try(:name) %></td>
          <td><%= link_to 'Destroy', destroy_details_employees_path(emp_id: @employee.id),class: 'btn btn-xs btn-primary' %></td>
        </tr>
    </tbody>
  </table>