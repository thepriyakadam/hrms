<%= form_for(@sidebar_tab) do |f| %>
  <% if @sidebar_tab.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sidebar_tab.errors.count, "error") %> prohibited this sidebar_tab from being saved:</h2>

      <ul>
      <% @sidebar_tab.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.check_box :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
