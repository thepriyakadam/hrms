<div class="box-header">
    <div class="pull-right">
    </div>
  </div><!-- /.box-header -->
  <div class="box-body">
  <div id="comments">
    <table id="example" class="table table-bordered table-hover table-condensed">
    <thead>
      <tr>
        <th>Employee Code</th>
        <th>Employee Code</th>
        <th>Well Faire</th>
    </tr>
    </thead>
    <tbody>
      <% @salaryslips.each do |slip| %>
        <% employee = slip.employee %>
        <% joining = employee.joining_detail %>
        <% bank = employee.employee_bank_detail %>
        <% workingday = slip.workingday %>
        <tr>
          <td><%= slip.employee.manual_employee_code %></td>
          <td><%= employee.try(:first_name) %> <%= employee.try(:middle_name) %> <%= employee.try(:last_name) %></td>
          <td>
          <% @deducted_salaryslip_items = slip.salaryslip_components.where(is_deducted: true,other_component_name: "Well Faire") %>
          <% @deducted_salaryslip_items.each do |item| %>
              <% if item.other_component_name == "Well Faire" %>
                <%= item.calculated_amount.round %>
              <% end %>
            <% end %>
          </td>
        </tr><!-- Row -->
      <% end %><!-- slip -->
   </tbody>
  </table>
  </div>

  <%= link_to " Excel Report ",wellfair_xls_salary_slip_ledgers_path(month: params[:wellfair][:month], year: params[:wellfair][:year],company_id: params[:wellfair][:company_id],company_location_id: params[:food_deduction][:company_location_id], format: "xls"), class: "btn btn-xl btn-success fa fa-print" %>
  <%= link_to " Print Wellfair ",wellfair_pdf_salary_slip_ledgers_path(month: params[:wellfair][:month], year: params[:wellfair][:year],company_id: params[:wellfair][:company_id],company_location_id: params[:food_deduction][:company_location_id], format: "pdf"), class: "btn btn-xl btn-info fa fa-print", :target => "_blank" %>
</div>

<script type="text/javascript">
  $(document).ready(function() {
    $('#example').DataTable( {
        "lengthMenu": [[10, 25, 50,100, -1], [10, 25, 50,100, "All"]]
    } );
} );
</script>