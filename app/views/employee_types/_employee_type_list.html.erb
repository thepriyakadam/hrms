<table id="example2" class="table table-bordered table-hover">
  <thead>
    <tr>  
      <th>Name</th>
      <th>Action</th>
    </tr>
  </thead>
  <tbody>
    <% @employee_types.each do |employee_type| %>
      <tr>
        <td><%= employee_type.name %></td>
        <td>
          <%= link_to '', edit_employee_type_path(employee_type),class: 'btn btn-success fa fa-edit',remote:true %>
          <%= link_to '',  employee_type, method: :delete, data: { confirm: 'Are you sure?',remote:true },class: 'btn btn-danger fa fa-close' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
