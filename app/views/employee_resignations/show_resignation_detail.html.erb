<div class="box">
  <div class="box-header">
  <h3 class="box-title">Resignation List</h3>
    <div class="pull-right">
      <%= link_to 'Print Report', print_resignation_detail_employee_resignations_path(format: 'pdf',emp_id: @employee_resignation_id.employee_id,resignation_id: @employee_resignation_id.id),class: "btn btn-xl btn-info fa fa-print" %>
     
    </div>
  </div>
  <div class="box-body">
    <table id="example2" class="table table-bordered table-hover table-condensed'">
      <thead>
        <tr>
          <th>Reporting Manager</th>
          <th>Resignation Date</th>
          <th>Reason</th>
          <th>Is Notice Period</th>
          <th>Notice Period</th>
          <th>Tentative Leaving Date</th>
          <th>Remark</th>
          <th>Exit Interview Date</th>
          <th>Resign Status</th>
      </thead>

      <tbody>
        <% @resignation_histories.each do |resignation_history| %>
          <tr>
            <td><%= resignation_history.try(:reporting_master).try(:employee).try(:first_name) %> <%= resignation_history.try(:reporting_master).try(:employee).try(:middle_name) %> <%= resignation_history.try(:reporting_master).try(:employee).try(:last_name) %></td>
            <td><%= resignation_history.resignation_date %></td>
            <td><%= resignation_history.reason %></td>
            <td><%= resignation_history.is_notice_period %></td>
            <td><%= resignation_history.notice_period %></td>
            <td><%= resignation_history.tentative_leaving_date %></td>
            <td><%= resignation_history.remark %></td>
            <td><%= resignation_history.exit_interview_date %></td>
            <td><%= resignation_history.resign_status %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>