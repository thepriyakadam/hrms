<table id="example2" class="table table-bordered table-hover table-condensed">
  <thead>
    <tr>  
      <th>Name</th>
      <th>Action</th>
    </tr>
  </thead>
  <tbody>
    <% @company_types.each do |company_type| %>
      <tr>
        <td><%= company_type.name %></td>
        <td>
          <%= link_to '', edit_company_type_path(company_type),class: 'btn btn-sm btn-success fa fa-edit',remote:true %>
          <%= link_to '',  company_type, method: :delete, data: { confirm: 'Are you sure?',remote:true },class: 'btn btn-sm btn-danger fa fa-close' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
