<p id="notice"><%= notice %></p>

<h1>Listing Travel Expences</h1>

<table>
  <thead>
    <tr>
      <th>Travel request</th>
      <th>Total advance amount</th>
      <th>Total expence amount</th>
      <th>Remaining amount</th>
      <th>Employee amount</th>
      <th>Company amount</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @travel_expences.each do |travel_expence| %>
      <tr>
        <td><%= travel_expence.travel_request %></td>
        <td><%= travel_expence.total_advance_amount %></td>
        <td><%= travel_expence.total_expence_amount %></td>
        <td><%= travel_expence.remaining_amount %></td>
        <td><%= travel_expence.employee_amount %></td>
        <td><%= travel_expence.company_amount %></td>
        <td><%= link_to 'Show', travel_expence %></td>
        <td><%= link_to 'Edit', edit_travel_expence_path(travel_expence) %></td>
        <td><%= link_to 'Destroy', travel_expence, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Travel expence', new_travel_expence_path %>
