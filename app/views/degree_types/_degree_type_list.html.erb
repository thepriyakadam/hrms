<table id="example2" class="table table-bordered table-hover">
  <thead>
    <tr>  
      <th>Name</th>
      <th>Action</th>
    </tr>
  </thead>
  <tbody>
    <% @degree_types.each do |degree_type| %>
      <tr>
        <td><%= degree_type.name %></td>
        <td>
          <%= link_to '', edit_degree_type_path(degree_type),class: 'btn btn-success fa fa-edit',remote:true %>
          <%= link_to '',  degree_type, method: :delete, data: { confirm: 'Are you sure?',remote:true },class: 'btn btn-danger fa fa-close' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
