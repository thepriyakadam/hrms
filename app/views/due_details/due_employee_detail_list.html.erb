<div class="box">
<div class="box-header">
<h3 class="box-title">Due Details For : <%= full_name(@due_employee_details.first.employee) %> , Template Name : <%= @due_employee_details.first.due_template.try(:name) %><br>
</h3>
<div class="box-body">
<table id="example2" class="table table-bordered table-hover table-condensed">
  <thead>
    <tr>
     <th>ID</th>
     <th>No Due Owner</th>
     <th>Status Of Request</th>
     <th>Action</th>
<!--      <th>Download</th>
 --><!--      <th>Action</th>
 -->    </tr>
  </thead>

  <tbody>
    <% @due_employee_details.each do |due_employee_detail| %>
      <tr>
<!--        <td><= full_name(due_employee_detail.reporting_master.try(:employee)) %></td>
 -->
      <td><%= due_employee_detail.reporting_master.employee.try(:manual_employee_code) %></td>      
      <td><%= full_name(due_employee_detail.reporting_master.employee) %></td>
      <td><%= %></td>
<!--       <td><= due_employee_detail.try(:due_detail).try(:due_template).try(:name) %></td>
     <td>
           < if due_employee_detail.try(:due_detail).try(:due_template).try(:document_file_name) == nil %>
           <small class="label label-default">Document Not Available</small>
             < else %>
             <= link_to ' Document',download_due_tempalte_documents_path(id: due_employee_detail.due_detail.due_template.id),class: 'btn btn-xs btn btn-info fa fa-download' %>
             < end %>
        </td> -->
       <td>
          <%= link_to ' View Due Action', specific_due_action_list_due_details_path(due_employee_detail),class: 'btn btn-xs btn-primary' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
<%= render 'due_action_list' %>
</div>
</div>