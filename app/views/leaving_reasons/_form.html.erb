<%= form_for(@leaving_reason) do |f| %>
  <% if @leaving_reason.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@leaving_reason.errors.count, "error") %> prohibited this leaving_reason from being saved:</h2>

      <ul>
      <% @leaving_reason.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
