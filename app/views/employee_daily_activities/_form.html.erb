<%= form_for(@employee_daily_activity) do |f| %>
  <% if @employee_daily_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employee_daily_activity.errors.count, "error") %> prohibited this employee_daily_activity from being saved:</h2>

      <ul>
      <% @employee_daily_activity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :employee_id %><br>
    <%= f.text_field :employee_id %>
  </div>
  <div class="field">
    <%= f.label :project_master_id %><br>
    <%= f.text_field :project_master_id %>
  </div>
  <div class="field">
    <%= f.label :today_activity %><br>
    <%= f.text_area :today_activity %>
  </div>
  <div class="field">
    <%= f.label :tomorrow_plan %><br>
    <%= f.text_area :tomorrow_plan %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
