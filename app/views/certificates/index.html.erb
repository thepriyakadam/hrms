<div class="box">
  <div class="box-header">
    <h3 class="box-title">Certificate List</h3>
    <div class="pull-right">
      <%= link_to "Create New Certificate Format", new_certificate_path, class: 'btn btn-sm btn-primary' %> 
    </div>
  </div><!-- /.box-header -->
  <div class="box-body">
    <table id="example2" class="table table-bordered table-hover table-condensed">
    <thead>
       <tr>
      <th>Certificate master</th>
      <th>Action</th>
      </tr>
    </thead>
    <tbody>
         <% @certificates.each do |certificate| %>
      <tr>
        <td><%= certificate.certificate_master.try(:name) %></td>
        <td><%= link_to 'Show', certificate %>
        <%= link_to 'Edit', edit_certificate_path(certificate) %>
        <%= link_to 'Destroy', certificate, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
  </div>
</div>
</div>
