exit
EmployeeLeavBalance.where(id: @employee_leave_balance).update_all(is_confirm: true)
@employee_leav_balance = EmployeeLeavBalance.find(params[:emp_leav_bal_id])
<%= bootstrap_form_for :goal_rating,url:{action: "update_goal_set_modal",id: @goal_rating.id} do |f| %><%= bootstrap_form_for :goal_rating,url:{action: "update_goal_set_modal",id: @goal_rating.id} do |f| %><%= bootstrap_form_for :goal_rating,url:{action: "update_goal_set_modal",id: @goal_rating.id} do |f| %><%= bootstrap_form_for :goal_rating,url:{action: "update_goal_set_modal",id: @goal_rating.id} do |f| %><%= bootstrap_form_for :goal_rating,url:{action: "update_goal_set_modal",id: @goal_rating.id} do |f| %>
exit
tp @goal_rating = GoalRating.find(params[:format])
@goal_rating = GoalRating.find(params[:format])
exit
params["goal_type"] == "Goal"
params["goal_type"] == "Attribute"
params["goal_type"]
@dropdown == "Attribute"
@dropdown == "Goal"
@dropdown
