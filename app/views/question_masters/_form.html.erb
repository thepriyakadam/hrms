<%= form_for(@question_master) do |f| %>
  <% if @question_master.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@question_master.errors.count, "error") %> prohibited this question_master from being saved:</h2>

      <ul>
      <% @question_master.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
