<div class="box-header">   
<div class="box-body">
<table  class="table table-bordered table-hover table-condensed'">
      <thead>
        <tr>
        <th>Sr.No</th>
        <th>Approver ID</th>
        <th>Approver Name</th>
        <th>Travel Request Status</th>
        <th>Comment</th>
        <th>Expence Claim Status</th>
       </tr>
      </thead>

      <tbody>
        <% i = 0 %>
        <% @reporting_masters_travel_requests1.each do |a| %>
          <tr>
            <td><%= i=i+1 %></td>
            <td><%= a.reporting_master.employee.manual_employee_code %></td>
            <td><%= a.reporting_master.employee.try(:first_name) %> <%= a.reporting_master.employee.try(:middle_name) %> <%= a.reporting_master.employee.try(:last_name) %></td>
  	        <td><%= a.travel_status %></td>
            <td><%= a.daily_bill_comment %></td>
            <td>
              <% if a.status == true %>
              <small class="label label-success">Confirmed & Send Next</small>
             <!--  < if @dbl = ReportingMastersTravelRequest.where(travel_request_id: @travel_request.id).pluck(:status).last == true %>
              <small class="label label-danger">Confirmed</small>
              < else %>
              <s end %> -->
              <% elsif a.status == false %>
              <small class="label label-danger">Rejected</small>
              <% else %>
              <% end %>
  	        </td>
          </tr>
        <% end %>
      </tbody>
</table>
</div>
</div>