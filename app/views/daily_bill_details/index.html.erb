<p id="notice"><%= notice %></p>

<h1>Listing Daily Bill Details</h1>

<table>
  <thead>
    <tr>
      <th>Travel request</th>
      <th>Expence date</th>
      <th>E place</th>
      <th>Travel expence</th>
      <th>Local travel expence</th>
      <th>Lodging expence</th>
      <th>Boarding expence</th>
      <th>Other expence</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @daily_bill_details.each do |daily_bill_detail| %>
      <tr>
        <td><%= daily_bill_detail.travel_request %></td>
        <td><%= daily_bill_detail.expence_date %></td>
        <td><%= daily_bill_detail.e_place %></td>
        <td><%= daily_bill_detail.travel_expence %></td>
        <td><%= daily_bill_detail.local_travel_expence %></td>
        <td><%= daily_bill_detail.lodging_expence %></td>
        <td><%= daily_bill_detail.boarding_expence %></td>
        <td><%= daily_bill_detail.other_expence %></td>
        <td><%= link_to 'Show', daily_bill_detail %></td>
        <td><%= link_to 'Edit', edit_daily_bill_detail_path(daily_bill_detail) %></td>
        <td><%= link_to 'Destroy', daily_bill_detail, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Daily bill detail', new_daily_bill_detail_path %>
