<table id="example2" class="table table-bordered table-hover table-condensed'">
  <thead>
    <tr>  
      <th>Name</th>
      <th>Action</th>
    </tr>
  </thead>
  <tbody>
     <% @employee_designations.each do |employee_designation| %>
      <tr>
        <td><%= employee_designation.try(:name) %></td>
        <td>
          <%= link_to '', edit_employee_designation_path(employee_designation),class: 'btn btn-sm btn-success fa fa-edit fa-sm',remote:true %>
          <%= link_to '',  employee_designation, method: :delete, data: { confirm: 'Are you sure?',remote:true },class: 'btn btn-sm btn-danger fa fa-close fa-sm' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
