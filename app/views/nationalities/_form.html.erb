<%= bootstrap_form_for(@nationality,html: {id: 'nationality'},remote:true) do |f| %>
  <div class="row">
    <div class="col-sm-3">
      <div class="form-group required">
        <%= f.text_field :name,hide_label:true, placeholder: 'Nationality' %>
      </div>
    </div>

    <div class="col-sm-3">
      <div class="actions">
        <%= f.submit :class => class_name%>
      </div>
    </div>
  </div>
<% end %>

<script>
$("#nationality").validate({
    rules: {
      "nationality[name]":{
        required: true,
      }
       },
    messages: {
      "nationality[name]":{
        required: "Please Specify Nationality",
      }
       },
    errorPlacement: function(error, element) {
     error.css({"color": "red", "font-size": "12px","font-weight" : "normal"})
     error.insertAfter(element.parent(element));
    }
   });
</script>


