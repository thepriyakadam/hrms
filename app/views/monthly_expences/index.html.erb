<div class="box">
  <div class="box-header">
    <h3 class="box-title">Employee Expences</h3>
    <div class="pull-right">
      <%= link_to "New Monthly expence", new_monthly_expence_path, class: 'btn btn-sm btn-primary' %> 
    </div>
    
  </div><!-- /.box-header -->
  <div class="box-body">
  <div id="comments">
    <table id="example1" class="table table-bordered table-hover table-condensed">
    <thead>
      <tr>
        <th>Year</th>
        <th>Action</th>
      </tr>
    </thead>

    <tbody>
       <% @monthly_expences.each do |w| %>
        <tr>
          <td><%= w.year %></td>
          <td>
            <%= link_to 'January',employees_workingdays_path(year: w.year, month: "January") , class: 'btn btn-xs btn-primary' %>
            <%= link_to 'February',employees_workingdays_path(year: w.year, month: "February") , class: 'btn btn-xs btn-primary' %>
            <%= link_to 'March',employees_workingdays_path(year: w.year, month: "March") , class: 'btn btn-xs btn-primary' %>
            <%= link_to 'April',employees_workingdays_path(year: w.year, month: "April") , class: 'btn btn-xs btn-primary' %>
            <%= link_to 'May',employees_workingdays_path(year: w.year, month: "May") , class: 'btn btn-xs btn-success' %>
            <%= link_to 'June',employees_workingdays_path(year: w.year, month: "June") , class: 'btn btn-xs btn-success' %>
            <%= link_to 'July',employees_workingdays_path(year: w.year, month: "July") , class: 'btn btn-xs btn-success' %>
            <%= link_to 'August',employees_workingdays_path(year: w.year, month: "August") , class: 'btn btn-xs btn-success' %>
            <%= link_to 'September',employees_workingdays_path(year: w.year, month: "September") , class: 'btn btn-xs btn-danger' %>
            <%= link_to 'October',employees_workingdays_path(year: w.year, month: "October") , class: 'btn btn-xs btn-danger' %>
            <%= link_to 'November',employees_workingdays_path(year: w.year, month: "November") , class: 'btn btn-xs btn-danger' %>
            <%= link_to 'December',employees_workingdays_path(year: w.year, month: "December") , class: 'btn btn-xs btn-danger' %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
  </div>
</div>
</div>
