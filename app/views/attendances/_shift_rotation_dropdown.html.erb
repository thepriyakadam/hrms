<%= select :attendance,:attendance_date, (@shift_rotations.present? ? (@shift_rotations.collect {|b| [b.start_date.to_s+" TO "+b.end_date.to_s,b.id]}) : {}),{prompt: true},{onchange: "var a = {shift_rotation_id : $(this).val()};$.get('/attendances/collect_employee',a,function(response){});",class: 'form-control'} %>