<table id="example2" class="table table-bordered table-hover">
  <thead>
    <tr>  
      <th>Name</th>
      <th>Action</th>
    </tr>
  </thead>
  <tbody>
    <% @employee_grades.each do |employee_grade| %>
      <tr>
        <td><%= employee_grade.name %></td>
        <td>
          <%= link_to '', edit_employee_grade_path(employee_grade),class: 'btn btn-success fa fa-edit',remote:true %>
          <%= link_to '',  employee_grade, method: :delete, data: { confirm: 'Are you sure?',remote:true },class: 'btn btn-danger fa fa-close' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
