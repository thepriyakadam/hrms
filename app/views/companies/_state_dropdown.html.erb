<label class="control-label required" for="company_state" aria-required="true">State</label>

 <%= select :company,:state,(@states.present? ? (@states.collect {|k,v| [k,v]}) : {}),{prompt:"Select State",hide_label:true},{onchange: "var a={id:$(this).val()}; $.get('/companies/collect_cities',a,function(response){});", class: "form-control valid"}%>